#!/bin/bash

# --- 설정 ---
CONTAINER_NAME="dpm-arch"             # Distrobox 컨테이너 이름
BACKUP_REPO="${CONTAINER_NAME}-backup" # 백업 이미지가 저장될 레포지토리 이름
MAX_BACKUPS=1                         # 유지할 최대 백업 개수

action=$1

CMD=""
LOG_MSG=""

# 백업 정리 함수 (Rotation Logic)
cleanup_old_backups() {
    echo "🔍 Checking for old snapshots..."
    
    # 1. 해당 레포지토리의 이미지들을 생성 최신순으로 나열
    # 2. tail -n +$((MAX_BACKUPS + 1)) : 상위 N개를 건너뛰고 그 이후(오래된 것들)만 출력
    # 3. xargs -r podman image rm : 출력된 오래된 이미지들을 삭제 (목록이 없으면 실행 안 함)
    
    podman images --sort created --format "{{.Repository}}:{{.Tag}}" \
    | grep "${BACKUP_REPO}:" \
    | tail -n +$((MAX_BACKUPS + 1)) \
    | xargs -r podman image rm
    
    # 삭제 작업 후 결과 메시지 (선택 사항)
    # 삭제된 이미지가 없다면 위 명령어가 조용히 넘어갑니다.
}

# ---------------------------------------------------------
# 함수: 설치된 패키지에서 .desktop 파일을 찾아 자동 Export
# ---------------------------------------------------------
auto_export_apps() {
    local pkg_name="$1"
    echo "🔍 Checking for GUI applications in '$pkg_name'..."

    # 컨테이너 내부에서 실행할 스크립트
    # 1. pacman -Qlq: 패키지가 설치한 파일 목록 조회 (Quiet mode)
    # 2. grep: .desktop 파일만 필터링 (/usr/share/applications/ 경로)
    # 3. while read: 발견된 파일마다 distrobox-export 실행
    
    distrobox-enter "$CONTAINER_NAME" -- bash <<EOF
        # 패키지 이름 변수 설정 (호스트의 $pkg_name 값이 여기에 들어감)
        TARGET="$pkg_name"
        
        # pacman 쿼리 실행
        pacman -Qlq "\$TARGET" 2>/dev/null | grep -E '^/usr/share/applications/.*\.desktop$' | while read -r file; do
            # 파일이 실제로 존재하는지 확인
            if [ -f "\$file" ]; then
                app=\$(basename "\$file" .desktop)
                echo "🚀 Auto-exporting: \$app"
                distrobox-export --app "\$app"
            fi
        done
EOF
}

if [ "$action" == "init" ]; then
    echo "🛠 Initializing dpm environment..."
    
    # 1. 아치 리눅스 컨테이너 생성
    distrobox-create --name $CONTAINER_NAME --image docker.io/library/archlinux:latest --yes
    
    # 2. 시스템 설정 (로케일, Multilib, 병렬 다운로드)
    echo "⚙️  Configuring system..."
    
    distrobox-enter $CONTAINER_NAME -- sudo bash <<EOF
        # --- 1) 로케일 설정 ---
        sed -i 's/^#en_US.UTF-8 UTF-8/en_US.UTF-8 UTF-8/' /etc/locale.gen
        
        HOST_LANG="$LANG"
        if [ -n "\$HOST_LANG" ]; then
            echo "   -> Detected host locale: \$HOST_LANG"
            CLEAN_LANG=\$(echo "\$HOST_LANG" | sed 's/\./\\\./')
            sed -i "s/^#\$CLEAN_LANG/\$CLEAN_LANG/" /etc/locale.gen
        fi
        
        locale-gen

        # --- 2) Multilib 활성화 ---
        echo "   -> Enabling multilib repository..."
        
        # pacman.conf에 [multilib]이라는 글자가 있는지 확인
        if grep -q "^\[multilib\]" /etc/pacman.conf; then
            echo "      Found existing section. Uncommenting..."
            sed -i '/\[multilib\]/,/Include/s/^#//' /etc/pacman.conf
        else
            echo "      Section missing. Appending to file..."
            # 파일 끝에 [multilib] 설정 추가
            echo "" >> /etc/pacman.conf
            echo "[multilib]" >> /etc/pacman.conf
            echo "Include = /etc/pacman.d/mirrorlist" >> /etc/pacman.conf
        fi

        # --- 3) 병렬 다운로드 활성화 (속도 향상) ---
        echo "   -> Setting ParallelDownloads to 10..."
        # ParallelDownloads = 5  <-- 이 줄을 찾아서 ParallelDownloads = 10 으로 변경
        sed -i 's/^ParallelDownloads.*/ParallelDownloads = 10/' /etc/pacman.conf

        # 설정이 잘 되었는지 확인용 (로그 출력)
        echo "   -> Verifying pacman.conf changes:"
        grep -E "^\[multilib\]|^ParallelDownloads" /etc/pacman.conf
EOF

    # 3. 시스템 업데이트 및 필수 패키지 설치
    # Multilib이 활성화되었으므로 -Syu 실행 시 multilib DB도 함께 동기화됩니다.
    echo "📦 Setting up base packages and updating..."
    distrobox-enter $CONTAINER_NAME -- sudo pacman -Syu --noconfirm base-devel git

    # 4. yay 설치
    echo "📦 Setting up AUR helper (yay)..."
    distrobox-enter $CONTAINER_NAME -- bash -c "git clone https://aur.archlinux.org/yay-bin.git && cd yay-bin && makepkg -si --noconfirm && cd .. && rm -rf yay-bin"
    
    # 초기 상태 커밋 및 전환
    echo "💾 Saving initial state..."
    INIT_TAG="${BACKUP_REPO}:init-$(date +%Y%m%d%H%M%S)"
    podman commit "$CONTAINER_NAME" "$INIT_TAG" > /dev/null
    
    echo "🔄 Switching container to initial backup layer..."
    distrobox-stop "$CONTAINER_NAME" --yes
    distrobox-rm "$CONTAINER_NAME" --force
    distrobox-create --name "$CONTAINER_NAME" --image "$INIT_TAG" --yes

    echo "✨ Initialization complete! You can now use 'dpm install <package>'."
    exit 0
fi

if [ "$action" == "self-update" ]; then
    echo "🔄 Updating dpm..."
    sudo curl -fsSL https://raw.githubusercontent.com/hanch2396/dpm/main/dpm -o /usr/local/bin/dpm
    sudo chmod +x /usr/local/bin/dpm
    echo "✅ Updated to the latest version."
    exit 0
fi

shift  # 첫 번째 인자(install, upgrade 등)를 목록에서 제거
       # 이제부터 $1, $2... 는 모두 패키지 이름이 됩니다. ($@)

if [ "$action" == "install" ]; then
    # 인자가 없으면 에러
    if [ $# -eq 0 ]; then
        echo "❌ Error: Package name(s) required."
        exit 1
    fi
    
    # $* : 남은 모든 인자를 공백으로 연결 (예: "pkg1 pkg2 pkg3")
    CMD="yay -S $* --noconfirm"
    LOG_MSG="📦 Installing packages: $*"

elif [ "$action" == "remove" ]; then
     if [ $# -eq 0 ]; then
        echo "❌ Error: Package name(s) required."
        exit 1
    fi
    CMD="yay -Rns $* --noconfirm"
    LOG_MSG="🗑 Removing packages: $*"

elif [ "$action" == "upgrade" ]; then
    # 시스템 전체 업데이트 (Arch의 표준 방식: -Syu)
    CMD="yay -Syu --noconfirm"
    LOG_MSG="🔄 Upgrading entire system..."

elif [ "$action" == "enter" ]; then
    # 사용법 1: dpm enter          (단순 셸 진입)
    # 사용법 2: dpm enter [명령어] (컨테이너 안에서 특정 명령어 실행)

    if [ $# -eq 0 ]; then
        # 추가 인자가 없으면 그냥 셸로 진입
        echo "🚀 Entering interactive shell in '$CONTAINER_NAME'..."
        echo "Type 'exit' to return to host."
        distrobox-enter "$CONTAINER_NAME"
    else
        # 추가 인자가 있으면 해당 명령어를 컨테이너 안에서 실행
        # -- (대시 두 개)는 distrobox 옵션과 실행할 명령어를 구분합니다.
        echo "🚀 Executing in '$CONTAINER_NAME': $*"
        distrobox-enter "$CONTAINER_NAME" -- "$@"
    fi
    
    # 실행 후 종료 코드 전달
    exit $?

elif [ "$action" == "export" ]; then
    # 수동 Export 기능
     if [ $# -eq 0 ]; then
        echo "❌ Error: App name required."
        exit 1
    fi
    distrobox-enter "$CONTAINER_NAME" -- distrobox-export --app "$1"
    exit $?

else
    # 알 수 없는 명령어 처리 (오타 방지)
    echo "❌ Unknown command: '$action'"
    echo "   Did you mean 'install'?"
    echo "Usage: dpm [init|install|remove|upgrade|enter|export] ..."
    exit 1
fi

# ---------------------------------------------------------
# 실행 로직 (install, remove, upgrade만 여기로 옴)
# ---------------------------------------------------------

# 0. 컨테이너 존재 확인 (안전을 위해)
if ! podman container exists "$CONTAINER_NAME"; then
    echo "❌ Container '$CONTAINER_NAME' not found."
    echo "   Please run 'dpm init' first."
    exit 1
fi

# 1. [Safety] 실행 전, 현재 상태를 무조건 백업 (Rollback Point)
#    이것이 '기존 이미지' 역할을 합니다.
PRE_TAG="${BACKUP_REPO}:pre-$(date +%Y%m%d%H%M%S)"
echo "💾 Creating safety snapshot (Pre-Execution): $PRE_TAG"
podman commit "$CONTAINER_NAME" "$PRE_TAG" > /dev/null

if [ $? -ne 0 ]; then
    echo "❌ Safety snapshot failed. Aborting to protect data."
    exit 1
fi

# 2. 명령어 실행 (설치 시도)
echo "$LOG_MSG"
distrobox-enter "$CONTAINER_NAME" -- $CMD
EXIT_CODE=$?

# 3. 결과에 따른 분기 처리
if [ $EXIT_CODE -eq 0 ]; then
    # ================================
    # 성공 시: 새로운 상태로 전진 (Forward)
    # ================================
    echo "✅ Success!"
    
    # 현재(설치된) 상태를 '새로운 이미지'로 저장
    POST_TAG="${BACKUP_REPO}:post-$(date +%Y%m%d%H%M%S)"
    echo "💾 Committing new state: $POST_TAG"
    podman commit "$CONTAINER_NAME" "$POST_TAG" > /dev/null
    
    echo "🔄 Recreating container with NEW image (Incremental)..."
    distrobox-stop "$CONTAINER_NAME" --yes
    distrobox-rm "$CONTAINER_NAME" --force
    distrobox-create --name "$CONTAINER_NAME" --image "$POST_TAG" --yes
    
    # 성공했으므로 자동 Export 실행
    if [ "$action" == "install" ]; then
        for pkg in "$@"; do
            auto_export_apps "$pkg"
        done
    fi

else
    # ================================
    # 실패 시: 기존 이미지로 후퇴 (Rollback)
    # ================================
    echo "⚠️  Operation failed!"
    echo "🔄 Rolling back to safety snapshot ($PRE_TAG)..."
    
    distrobox-stop "$CONTAINER_NAME" --yes
    distrobox-rm "$CONTAINER_NAME" --force
    distrobox-create --name "$CONTAINER_NAME" --image "$PRE_TAG" --yes
    
    echo "✅ Rollback completed. System is clean."
    exit 1
fi

# 4. 마무리: 오래된 백업 태그 정리
cleanup_old_backups
